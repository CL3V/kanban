{
  "watch": ["backend"],
  "ext": "ts,js,json",
  "ignore": ["backend/database/*.db"],
  "exec": "npx ts-node --project tsconfig.backend.json backend/server.ts",
  "env": {
    "NODE_ENV": "development"
  }
}
